# Day 12 - Undoing Changes

## Summary

- `git reset` removes commits or unstages changes by moving the branch pointer.
- `git revert` creates a new commit that reverses a previous commit.
- Use `reset` for local, unpublished commits.
- Use `revert` for commits that have already been shared.