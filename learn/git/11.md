# Day 11 - Cloning a Repository

## Summary

- `git clone` creates a local copy of an existing remote repository.
- It downloads the project files, commit history, and branches.
- The `.git` directory and `origin` remote are created automatically.
- `git clone` is the standard way to start working on an existing project.