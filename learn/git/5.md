# Day 05 - Branching

## Summary

- Branches allow independent development without affecting the main branch.
- `git branch` creates and manages branches.
- `git switch` is the recommended way to change branches.
- Changes can be merged back into the main branch after development is complete.