# Day 03 - Checking Status & History

## Summary

- `git status` shows the current state of the repository.
- `git log` displays the commit history.
- `git log --oneline` provides a compact commit list.
- `git log --graph` visualizes the branch history.