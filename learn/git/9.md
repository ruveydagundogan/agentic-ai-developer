# Day 09 - Pushing Changes

## Summary

- `git push` uploads local commits to a remote repository.
- Use `git push -u origin <branch>` when pushing a branch for the first time.
- After the upstream is set, `git push` is enough for future updates.
- Pushing allows others to access and collaborate on your changes.