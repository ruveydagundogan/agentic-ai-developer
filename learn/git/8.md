# Day 08 - Connecting to a Remote

## Summary

- A remote repository is hosted on a server, such as GitHub.
- `git remote add` connects a local repository to a remote repository.
- `git remote -v` lists the configured remote repositories.
- `origin` is the default name for the primary remote repository.