# Day 14 - Cherry-Picking

## Summary

- `git cherry-pick` applies a specific commit from one branch to another.
- It copies only the selected commit instead of merging the entire branch.
- It is useful for moving bug fixes or small changes between branches.