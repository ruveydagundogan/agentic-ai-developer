# Day 04 - Ignoring Files

## Summary

- `.gitignore` specifies files and folders that Git should ignore.
- It helps keep the repository clean by excluding unnecessary files.
- Common examples include build files, logs, IDE settings, and environment files.
- `.gitignore` only affects files that are not already tracked by Git.