# Day 07 - Resolving Merge Conflicts

## Summary

- A merge conflict occurs when Git cannot merge changes automatically.
- Conflicts usually happen when the same part of a file is modified in different branches.
- Resolve the conflict by editing the file manually.
- After resolving, use `git add` and `git commit` to complete the merge.