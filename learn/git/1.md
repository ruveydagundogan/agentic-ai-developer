# Day 01 - Initializing a Repository

## Summary

- `git init` creates a new Git repository.
- It generates a hidden `.git` directory.
- The `.git` folder stores Git history, branches, and configuration.
- `git clone` automatically includes the `.git` directory.